spring:
  application:
    name: Gateway API
  datasource:
    generate-unique-name: false
    url: jdbc:h2:mem:patentesdb;MODE=MySQL
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
